//Author: Nathan Gerbino
#include "colors.h"
#include <string>
#include <iostream>


using namespace std;


colors::colors(string color, string colorgenmethod, string filename, string personality) : color(color), colorgenmethod(colorgenmethod), filename(filename), personality(personality){
  color = color;
  colorgenmethod = colorgenmethod;
  filename= filename;
}